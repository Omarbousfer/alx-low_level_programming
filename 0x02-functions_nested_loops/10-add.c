#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: parametre of first integer
 * @b: parametre of second integer
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}
